'use client'

import { Button } from '@forge-ui-official/core'

export default function Page() {
  return (
    <main className="p-8">
      <Button onClick={() => {}}>Submit</Button>
    </main>
  )
}
