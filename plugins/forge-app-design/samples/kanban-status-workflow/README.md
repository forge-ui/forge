# kanban-status-workflow

Legacy alias for `kanban-workflow-protask`.

Use the canonical sample instead:

```text
$FORGE_APP_DESIGN_ROOT/samples/kanban-workflow-protask
```

This folder is kept only for older references. New pattern decisions, BRIEF
examples, page-pattern artifacts, IA artifacts, eval rules, and screenshot
acceptance should use `kanban-workflow-protask`.
